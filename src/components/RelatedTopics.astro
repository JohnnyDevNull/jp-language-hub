---
interface RelatedTopic {
	href: string;
	label: string;
	description?: string;
}

interface Props {
	topics?: RelatedTopic[];
	title?: string;
}

const { topics = [], title = 'Related topics' } = Astro.props;
---

{
	topics.length > 0 && (
		<nav class="related-topics" aria-label={title}>
			<p class="related-topics-title" aria-hidden="true">{title}</p>
			<ul class="related-topics-items">
				{topics.map((topic) => (
					<li class="related-topics-item">
						<a class="related-topics-link" href={topic.href}>
							<span class="related-topics-label">{topic.label}</span>
							{topic.description && (
								<span class="related-topics-description">{topic.description}</span>
							)}
						</a>
					</li>
				))}
			</ul>
		</nav>
	)
}
