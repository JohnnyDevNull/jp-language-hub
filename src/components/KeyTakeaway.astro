---
interface Props {
	title?: string;
}

const { title = 'In short' } = Astro.props;
---

<aside class="key-takeaway" aria-label={title}>
	<p class="key-takeaway-title" aria-hidden="true">{title}</p>
	<div class="key-takeaway-content"><slot /></div>
</aside>
